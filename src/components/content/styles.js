import { makeStyles } from '@material-ui/core/styles';

export const styles = makeStyles({
  content: {
    display: 'flex',
    flexAlign: 'center',
    justifyContent: 'center',
    background: '#efeff5',
  },
  contentMobile: {
    marginTop: 94,
    display: 'flex',
    flexAlign: 'center',
    justifyContent: 'center',
    background: '#efeff5',
  },
});
