import { makeStyles } from '@material-ui/core/styles';

export const styles = makeStyles({
  content: {
    display: 'flex',
    flexAlign: 'center',
    justifyContent: 'center',
    width: '100%',
  },
  contentMobile: {
    marginTop: 0,
    display: 'flex',
    flexAlign: 'center',
    justifyContent: 'center',
  },
});
