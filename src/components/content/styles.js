import { makeStyles } from '@material-ui/core/styles';

export const styles = makeStyles({
  content: {
    marginTop: 64,
    display: 'flex',
    flexAlign: 'center',
    justifyContent: 'center',
    background: '#fafafa',
  },
  contentMobile: {
    marginTop: 94,
    display: 'flex',
    flexAlign: 'center',
    justifyContent: 'center',
    background: '#fafafa',
  },
});
