import React from 'react';
import { useStyles } from './styles';

export default function ProfileCardBlank({}) {
  const classes = useStyles();

  return <div className={classes.profileCardBlank}></div>;
}
