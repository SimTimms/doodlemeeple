import { makeStyles } from '@material-ui/core/styles';

export const styles = makeStyles({
  container: {
    display: 'flex',
    flexWrap: 'wrap',
    justifyContent: 'center',
    alignItems: 'center',
  },
  navBar: {
    background: '#fff',
  },
  content: {
    marginTop: 64,
    display: 'flex',
    flexAlign: 'center',
    justifyContent: 'center',
  },
  textField: {
    marginLeft: 10,
    marginRight: 10,
    width: '100%',
  },
  dense: {
    marginTop: 16,
  },
  menu: {
    width: 200,
  },
});
