import { makeStyles } from '@material-ui/core/styles';

export const styles = makeStyles({
  navBar: {
    background: '#fff',
  },
  content: {
    marginTop: 64,
    display: 'flex',
    flexAlign: 'center',
    justifyContent: 'center',
  },
  dense: {
    marginTop: 16,
  },
  menu: {
    width: 200,
  },
});
