import { makeStyles } from '@material-ui/core/styles';

const drawerWidth = 160;

export const useStyles = makeStyles((theme) => ({
  root: { height: '100vh', background: '#efeff5', width: '100%' },
}));
